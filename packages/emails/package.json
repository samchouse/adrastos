{
  "name": "@adrastos/emails",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "email dev",
    "export": "email export",
    "worker:dev": "run -T conc \"cd lib && run -T tsc -w\" \"run -T nodemon out/lib/worker.js\"",
    "worker:build": "cd lib && run -T tsc",
    "worker:start": "node out/lib/worker.js"
  },
  "dependencies": {
    "@react-email/components": "0.0.6",
    "@t3-oss/env-core": "^0.4.0",
    "dotenv": "^16.1.3",
    "pino": "^8.14.1",
    "react-email": "1.9.3",
    "redis": "^4.6.7",
    "zod": "^3.21.4"
  }
}
