{
  "name": "@adrastos/emails",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "run -T conc \"cd lib && run -T tsc -w\" \"run -T nodemon out/lib/worker.js\"",
    "build": "cd lib && run -T tsc",
    "start": "node out/lib/worker.js",
    "email:dev": "email dev",
    "email:export": "email export"
  },
  "dependencies": {
    "@react-email/components": "0.0.7",
    "@t3-oss/env-core": "^0.6.0",
    "dotenv": "^16.3.1",
    "pino": "^8.15.0",
    "react-email": "1.9.4",
    "redis": "^4.6.7",
    "zod": "^3.21.4"
  }
}
