server {
	http2 on;
	listen 443 ssl;
	listen [::]:443 ssl;
	server_name adrastos-pr-*.xenfo.dev;

	# SSL
	ssl_certificate /etc/letsencrypt/live/*.xenfo.dev/fullchain.pem;
	ssl_certificate_key /etc/letsencrypt/live/*.xenfo.dev/privkey.pem;
	ssl_trusted_certificate /etc/letsencrypt/live/*.xenfo.dev/chain.pem;

	# security
	include nginxconfig.io/security.conf;

	# reverse proxy
	location / {
		proxy_pass http://staging-nginx:7000;
		proxy_set_header Host $host;
		include nginxconfig.io/proxy.conf;
	}

	# additional config
	include nginxconfig.io/general.conf;
}

# HTTP redirect
server {
	listen 80;
	listen [::]:80;
	server_name adrastos-pr-*.xenfo.dev;

	location / {
		return 301 https://$host$request_uri;
	}
}
